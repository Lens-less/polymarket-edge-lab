# Archived deployment assets

> [!WARNING]
> `deploy/` is retained only to reproduce historical public-data and paper experiments. The BTC 5m/15m structural track is `STRUCTURAL_STOP`; these assets are not a supported v0.1.0 deployment path and must not be enabled as a route to live trading.

The subdirectories preserve systemd units, bootstraps and watcher configuration referenced by research reports. Some files still describe their original activation procedure so the old environment can be audited. That text is historical, not current authorization.

Do not run these bootstraps on a production host, add credentials, or re-enable retired services without a new project, threat review and explicit deployment decision. New orders remain unconditionally blocked in source code.
