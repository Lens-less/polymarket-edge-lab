[build-system]
requires = ["setuptools>=61.0", "wheel"]
build-backend = "setuptools.build_meta"

[project]
name = "polymarket-mm-bot"
version = "0.1.0"
description = "Public-data-only Polymarket edge discovery and execution replay lab"
readme = "README.md"
license = {text = "MIT"}
requires-python = ">=3.11"
authors = [
    {name = "Polymarket Bot Developer"},
]
keywords = ["polymarket", "trading", "market-making", "bot"]
classifiers = [
    "Development Status :: 3 - Alpha",
    "Intended Audience :: Developers",
    "License :: OSI Approved :: MIT License",
    "Programming Language :: Python :: 3",
    "Programming Language :: Python :: 3.11",
    "Programming Language :: Python :: 3.12",
    "Topic :: Office/Business :: Financial :: Investment",
]

dependencies = [
    "polymarket-client==0.6.0",
    "python-dotenv",
    "websockets",
    "python-socks[asyncio]",
    "pandas",
    "pyarrow",  # For Parquet export
    "certifi",
    "requests",
    "rich>=13.0.0",
]

[project.optional-dependencies]
dev = [
    "pytest",
    "pytest-asyncio",
    "pytest-timeout",
]

[project.urls]
Homepage = "https://github.com/Lens-less/poly-mm"
Repository = "https://github.com/Lens-less/poly-mm"

[tool.setuptools.packages.find]
where = ["."]
include = ["src*"]

[tool.pytest.ini_options]
testpaths = ["tests"]
asyncio_mode = "auto"
timeout = 30
addopts = "-v --tb=short -m 'not network'"
markers = [
    "network: tests that require live external network access (run explicitly with -m network)",
]

[tool.isort]
profile = "black"
line_length = 100

[tool.black]
line-length = 100
target-version = ['py311']
