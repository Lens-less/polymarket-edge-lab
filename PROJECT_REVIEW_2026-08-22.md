# 项目全面审查报告（2026-08-22）

> **后续复核（同日）：本文关于“rewards 是最短盈利路径、12 USDC 先行探针、全站奖励池约 7,577 美元/天”的建议已被账户级盈利证据审计、资本约束复算与地理准入核查推翻，不应执行。最终 go/no-go 决策见 `STRATEGY_DECISION_2026-08-22.md`。本文保留为审查过程记录。**

> 审查方式：5 个独立子代理并行深入审查——①策略证据与结论、②策略核心代码（`src/edge_lab/`）、③基础设施代码（`src/` 除 edge_lab）、④部署运维（`deploy/aws/` + 服务运行时）、⑤测试体系（实际运行 1,792 个用例）。覆盖约 10 万行 Python、20+ 份研究文档、最近 30 个提交。
>
> 用户诉求基准：**争取早日实盘盈利，而不是追求完美、不是为了工程本身。** 本文所有"做/不做"的取舍均以此为准。

---

## 总判断

1. **最大问题不是代码，是策略：BTC 5m/15m 结构线已被算术和实测双重判死。**
   floor 定价是恒等式（双腿成本 = `1 + b`，`b ≥ 0` 是结算恒等式），1,017 个真实盘口样本里正 floor 出现 0 次；即使翻正，乐观上界也只有约 $29/天，低于项目自定的 $20/天 × 14 天商业门槛。**继续给这条线投工程是沉没成本。**

2. **代码质量远高于一般个人量化项目。** 策略核心链路（定价、回放、结算、防自欺证据链）没有发现致命的前视偏差或数学错误；实盘硬门（`compatibility.py`）真实有效。真问题集中在两处：
   - **基础设施层（feed/risk/模拟器）的一批真 bug**（断线不撤单、风控自炸、熔断不含浮亏等）；
   - **运维的两个已爆雷的坑**：SNS 告警 0 订阅、t3.small 主机过载——后者已实际吃掉 v07 轨道 14/15 的采集数据。

3. **离实盘盈利的最短路径不是把结构线修完，而是转向流动性奖励（rewards）线，并把 12 USDC 真金探针从流程终点挪到流程起点。** 诚实估算：约 **3–5 周到"知道能不能赚"**，而不是"已经赚钱"。

4. **CLAUDE.md 里"实盘通道物理上不存在"的表述失实。** 那是仓库自己写的安全闸（`src/edge_lab/compatibility.py:49-63` 无条件 raise），不是市场或 API 的物理限制；官方 SDK（polymarket-client 0.6.0）底座已装好，签名/审批由 SDK 内部完成。真实状态是："没有值得下单的策略 + 按合同不允许在判定前下单"。

---

## 一、问题清单

### 第 1 层：策略本身（致命级——决定一切）

| # | 问题 | 证据 | 影响 |
|---|------|------|------|
| S1 | **结构线无 edge，是恒等式层面的不可盈利** | `CLAUDE_FINAL_ASSESSMENT.md` §1：双 maker 口径需要 `b < 双腿价差之和(0.02)`，实测存活窗口 `b` 最小 0.062、中位 0.41；1,017 样本正 floor 0 次；三种执行口径（双 taker / maker+FOK / 双 maker）全部算过，无一可行 | 这条线天花板 ≈$29/天，连商业门槛都过不了 |
| S2 | **容量问题比显著性问题更致命** | v08 §7：即使每对赚 0.002（比实测最好值乐观）、每 expiry 配 150 对，也只有 ≈$29/天，占本金 $500–2,000 | 即使 Gate 0 翻正也不是生意 |
| S3 | **"实盘通道物理不存在"是修辞失实** | `compatibility.py:49-63` 是仓库自设安全闸；SDK 底座完整 | 心理上高估了实盘距离；正确方向是"找到值得下单的策略"而非"打通物理通道" |
| S4 | **全项目 `validated_profitable = 0`**（edge_discovery FINAL_REPORT） | 天气线判负（市场 Brier 0.177–0.150 vs 模型 0.288/0.206，双输）、Neg-Risk 1,267 候选全部 fail-closed、延迟线基本未开始 | 唯一有真实资金来源（补贴，全站奖励池实测 $7,577/天）的是 rewards 线；但 0 个市场过 preflight，已知主风险是单腿逆向选择（回放实测中位损失 $0.28–1.90/事件 vs 配对毛利 $1.00） |

### 第 2 层：策略核心代码（总体过硬，3 个中等问题）

主链路（v0.7/v0.8 定价、TWAP 结算、排队回放、rolling journal、硬门）经逐项验证**没有**前视偏差、符号错误、结算边界错误（`closing >= opening` 统一正确）、窗口 off-by-one、Decimal 污染。Gate 0 的 maker 成交假设只用于明示的 upper bound，不可能被当成实盘期望。需要修的：

| # | 严重度 | 问题 | 位置 | 影响 |
|---|--------|------|------|------|
| C1 | 中 | v0.6 回放在执行面缺失时**静默丢弃已决策/单腿已成交的 cycle**，含本应产生裸露损失的样本 | `src/edge_lab/btc_twap_relative_value_replay.py:845-889` | 历史 OOS PnL **系统性高估**（v0.7 的 `causal_no_fill_zero_pnl` 做法正确，v0.6 没同步） |
| C2 | 中 | `economics.py` 费用舍入不用主链路的"max(聚合，分层)"悲观规则 | `src/edge_lab/economics.py:30-48, 294-317` vs `execution.py:1021-1041` | 诊断性净 edge 轻微高估（不影响 BTC TWAP 主链路） |
| C3 | 中 | `select_healthy_pair_books` 非 structural 模式对 maker 腿 bid 无价格带检查 | `src/edge_lab/btc_twap_pair_pricing.py:293-308` | 未来复用会在深度虚值处高估 floor（当前仅 Gate 0 上限用途，无害） |

轻微项（不阻塞决策，修的时候顺手带走）：`signal_books` 取 `eligible[-1]` 乱序取书（`replay.py:544-569`）；`build_paired_bid_quote` 的 post-only 回退在对手 ask 已到最小 tick 时失效（`economics.py:102-106`）；terminal TWAP 等权 1 秒近似是模型选择而非 bug（`btc_twap_relative_value_v07.py:942-1001`）。

### 第 3 层：基础设施代码（半成品 + 一批真 bug）

**状态判定**：这套 feed/risk/orders/simulator 是"刻意限位的半成品"——LIVE 下单被硬门拦住（设计如此）；DRY_RUN 撮合链路断裂（`simulator.check_fills` 无任何生产调用方）；没有主交易循环把它们串起来。SDK 封装与 polymarket-client 0.6.0 逐项核对无误（含派生凭证、L1/L2 签名、审批设置）。

高危（任何未来用途都需要修）：

| # | 问题 | 位置 | 影响 |
|---|------|------|------|
| I1 | `ConnectionClosedOK` 不触发撤单保护回调、也不重连 | `src/feed/websocket_conn.py:164-167` | 服务器主动断线时"断线即撤单"不生效——实盘硬底线缺口 |
| I2 | 无应用层 PING（Polymarket 要求 ≤10s 文本 PING） | `src/feed/websocket_conn.py:79-85` | 静市场被反复踢线 → 撤单/重连风暴 |
| I3 | `risk.check()` 在 LIVE 下 API 故障时**抛异常而不是返回 STOP** | `src/risk/manager.py:310-311` | 最需要风控的时刻风控自炸；进程若退出，挂单留场内 |
| I4 | 日损熔断不含未实现盈亏；多市场未实现盈亏被单值覆盖（统计本身错误） | `src/risk/manager.py:277-298, 515-542` | 真金下熔断触发远慢于设计 |
| I5 | `TradesPoller` 在 asyncio 事件循环里做同步 HTTP | `src/feed/trades_poller.py:100-147` | 每轮冻结事件循环数百 ms，行情决策延迟尖峰 |
| I6 | trades 去重游标在高流量下重放重复成交、首轮回放历史 | `src/feed/trades_poller.py:128-144` | 订单流信号虚假放量 |
| I7 | WS 序列缺口状态黏滞：`is_healthy` 可能永久 False 且不触发 REST 备用 | `src/feed/data_store.py:141-156`、`src/feed/feed.py:321-328` | 策略静默停摆直到重启 |
| I8 | 持久化每秒写全深度快照、无保留策略、每行单独 commit | `src/feed/persistence.py:455-493` | 磁盘无限增长 + 写路径拖慢 |
| I9 | `simulator.check_fills` 无生产调用方、费用公式与 LIVE 不一致、无排队/部分成交 | `src/simulator.py:87-139` | paper 链路断裂且成交假设乐观（已被测试锁成"正确行为"） |
| I10 | `FillFeed`（用户通道成交推送）协议实现错误（鉴权方式不对）且无人用 | `src/feed/fill_feed.py:90-100` | 半成品；实盘感知成交目前只能靠轮询 |
| I11 | 回测引擎空头回补不实现盈亏、部分回补偷换成本价 | `src/backtest/engine.py:452-481` | 含空头策略的胜率/利润因子失真 |
| I12 | `place_order` 仓位检查不计未成交挂单 | `src/trading.py:189-207` | 连续下单可合计超 `MAX_POSITION_PER_MARKET` |

低危约 10 项（`markets.py` 全部请求无超时、`rate_limiter` 非线程安全、DRY_RUN 下 `get_trades` 排序随机化、`models.py` 用真值判断代替 `is not None` 等），不阻塞决策。

### 第 4 层：部署运维（"骨架前 1%，灵魂缺位"）

systemd 单元的安全硬化（ProtectSystem=strict、IMDS 封锁、空 capability 集、内容寻址发布、chrony 锁 Amazon Time Sync）在个人项目里是顶级水平。但：

| # | 严重度 | 问题 | 后果 |
|---|--------|------|------|
| D1 | **严重** | SNS 告警 0 个已确认订阅（`deploy/aws/watch/watch-config.json` 的 `sns_topic_arn` 为空） | v07 采集失败 93.3% 连续数天无人知道——**这已经实际发生** |
| D2 | **严重** | 一台 t3.small（eu-west-1）挤 6 个轨道：CPU credit ≈0、磁盘距软停仅 148 MiB、rawcap 日写 5.3 GiB | 93.3% 采集失败的根因（`capacity_backpressure`）；WP-E2 已判定换主机但**未执行**，v08 至今未上线 |
| D3 | **严重** | 磁盘策略数学矛盾：30 天保留 × 5.3 GiB/天 vs 15 GiB 软停 | 实际是"写满才停"，不是"防止写满" |
| D4 | 中 | 最近 30 个提交约 1/3 在修运维/采集可靠性、1/3 在修证据诚实性、1/4 在预建实盘安全 | 工程时间花在"诚实记录失败"而非"验证策略赚钱"；采集不稳直接顺延 Gate 0 判决日期 |
| D5 | 低 | 部署仪式（40 位 SHA 内容寻址归档 + 双 revision marker + AES-256 S3 中转）对单人单机过度工程 | 每次发版高摩擦；已产生 7 份近乎复制的 systemd 模板 |

好消息：服务器上目前没有任何实盘凭证（所有单元 `credentials_allowed=false`）；时间同步做得好（chrony，offset ~13µs，每 cycle 实测时钟）。

### 第 5 层：测试

**1,792 用例：1,722 通过 / 2 失败 / 44 跳过**（3 分 54 秒）。强项是防自欺（locked shadow 防篡改、no-trade 留分母、硬门不可被布尔解锁、结算 fail-closed 都有测试锁住）。缺口：

- 2 个失败均为环境问题：`.venv` 里还装着 legacy `py-clob-client 0.34.6`，违反"只用官方统一 SDK"前提。红灯长期亮着会训练人忽略红灯。
- `trading.place_order` 的 LIVE 门**没有直接回归测试**（门本身被测住了，但下单函数入口没有——未来重构挪走门不会变红）。
- LIVE 下单真实路径（`src/trading.py:260` 之后）零测试——门一开就是零测试碰真钱。
- 旧 simulator 的乐观成交假设被测试锁成了"正确行为"（`tests/test_phase6.py:202-282`）。

---

## 二、解决方案（按"早日盈利"排序）

### 本周就做（合计约 2–3 天，全是便宜且必须的）

1. **打通 SNS 告警**（确认一个邮箱/短信订阅，半小时）。没有它，后面所有采集的失败都不可信。
2. **换掉 t3.small**：非 burstable（t3 unlimited 或 m 系）、≥2GB 内存、≥30GiB 磁盘，$15–30/月。新主机上只跑 **v08/v09 + watch**；停用 v04/v05/v07 轨道、暂停 rawcap 全帧采集（与 v08 compact capture 目的重叠，日省 5.3 GiB）。
3. **给结构线开死亡证明，然后停手**：跑一次正式 v0.9 Gate 0（3–4 天）作结案判决；**不做** S1 的 200-expiry 采集和 E0 双 maker 适配器——那是为已判负策略修路。
4. **修 venv**（卸载 py-clob-client）让 2 个红灯转绿；补 `place_order(DRY_RUN=False)` 被硬门拦截的一行回归测试。
5. **修 C1**（v0.6 回放丢弃样本偏差，照抄 v0.7 的 `causal_no_fill_zero_pnl` 做法）——影响所有历史回测数字的可信度。

### 转向 rewards 线时做（1–2 周）

6. **写通用 venue 适配器**（复用官方 SDK + split/merge + 用户 WS）——E0 本来就要做、任何策略都绕不过的唯一大块工程。
7. **把 12 USDC 真金探针作为 rewards 线的第一个实验**：≤$25 抵押、单市场、20 shares、$5 日损上限，目标锁定 Ankara 类近零成交市场（挂着吃 scoring、回避逆向选择）。理由：真实 order scoring 得分和 payout **只能**用真实挂单测到——纸面再采 7 天只是重复 edge_discovery 已做过的"无法证明"循环。
8. **修基础设施高危项 I1–I4**（断线撤单语义、应用层 PING、风控 fail-closed、熔断含浮亏）——探针的真金安全底线，约 2–3 天。

### 明确不做（过度工程，砍掉）

- 200-expiry 中性 shadow + rolling journal 哈希链收据体系
- `LiveReleaseCapability` 的 SHA 绑定链 + 6 类 evidence schema + 8+3 项故障演练（对 12 USDC 探针超规格；合并为一次实弹演练即可）
- 部署仪式降级为 git tag + rsync + bootstrap 校验哈希
- 监控双层（每轨道 healthcheck + 中央 watcher）收敛为单层 watcher + SNS
- `dynamic_short_crypto_*` 双运行时（13,000+ 行，不在任何部署里）冻结归档

---

## 三、到什么程度才能实盘上线

按"12 USDC 探针 → 加仓 → 常态化"分级，**不是一步到位**：

### 第一级：12 USDC 真金探针（最快 2–3 周后可开闸）

- [ ] SNS 告警打通 + 非 burstable 主机就位
- [ ] I1–I4 风控/断线底线修复 + LIVE 门回归测试
- [ ] pUSD/签名/终态对账一次真实验证（下第一单前，用最小单完成四项认证收据）
- [ ] 目标主机 geoblock 实测（本机曾连接超时，不能用代理假造）
- [ ] kill switch 持久化 + 二次人工确认
- [ ] 单市场、单对限制、日损 $5 上限

### 第二级：探针验证 scoring/payout 为正后加仓（再 1–2 周）

- [ ] 多 epoch 真实 payout 落账且 > 逆向选择损失
- [ ] 断线即撤单实测演练（含一腿成交一腿拒绝场景）

### 第三级：常态化运行

- [ ] 14 日 × $20/天实际盈利记录（预注册门槛；对 rewards 线合理，对结构线永远过不了——所以别再用它衡量结构线）

---

## 结语

瓶颈从来不是工程——这个项目的工程质量在个人量化项目里是前 1% 的水平（防自欺证据链尤其出色，它确实拦下过两次差点被当成盈利证据的假阳性）。瓶颈是**还没有一条有真实经济来源的策略**。

最快的盈利路径：**本周修好告警和主机 → 给结构线开死亡证明 → 全力转 rewards 线 → 用 12 USDC 真金买真实数据。**

如果 rewards 探针也判负：文档体系内没有第三条有真实资金来源的路径（天气、Neg-Risk、延迟、预测全部 insufficient_data 或判负），届时考虑退出这个市场也是理性决策，而非失败。

---

*审查方法附录：本文结论来自 5 个并行子代理的独立审查——策略证据（v02–v08 全链文档 + 原始数据抽验）、策略核心代码逐项数学验证、基础设施逐文件核对（含与已安装 SDK 源码比对）、部署单元与运行时审查、测试套件实跑。所有"检查过没问题"的正面结论（floor 数学、结算边界、窗口对齐、SDK 封装签名、撤单路径、回测无前视等）均出自逐项验证，非默认放行。*
